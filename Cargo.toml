[package]
name = "maze_walk"
version = "0.1.0"
authors = ["UnlimitedHugs <unlimited.hugs@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bevy = { version = "=0.5.0", default-features = false, features = ["dynamic"] }
miniquad = { git = "https://github.com/not-fl3/miniquad" }
bevy_miniquad = { path = "bevy_miniquad" }
rand = "0.8.4"
easer = "0.2.1"